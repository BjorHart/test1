
/*
int anomalyDetect() {

  if (abs(robotArmPos(1, true) - sensorValue(1)) > 20) {
    // Difference between desired robot position and sensor value
    // is(probably) larger than 20 degrees.
    // Do something
  }
  
}

int robotArmPos(int servo, bool functionFinished) {

 // When movement function is over 
 // return input value on function

 // If (functionFinished) {
 //   return servo
 // }
 //
  return 0;
}

int sensorValue(int sensor) {
  return 0;
}

// Need to get signal from Braccio when movement function is finished, then
// compare input value to the function with the sensor value. If difference maybe signal it 
// on sensor or servo
// Alternative is to send angle from Braccio for every degree increment and compare it. 

*/
